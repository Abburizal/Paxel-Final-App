<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External Pictures Directory untuk menyimpan foto AR -->
    <external-files-path
        name="ar_photos"
        path="Pictures/" />

    <!-- External Files Directory -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- External Cache Directory untuk temporary files -->
    <external-cache-path
        name="external_cache"
        path="." />

    <!-- Files Directory untuk app-specific files -->
    <files-path
        name="files"
        path="." />

    <!-- Cache Directory untuk temporary app files -->
    <cache-path
        name="cache"
        path="." />
</paths>